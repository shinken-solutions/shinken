# /etc/default/shinken
#$ETC$ is where we put all configuration files
#$VAR$ is where we put all variables files (pid, and all)
#$SCRIPTS_BIN$ is chere the launch scripts will be send


###PART FOR ARBITER
# location of the nagios configuration file
SHINKENCFG="$ETC$/nagios.cfg"

# We got 2 config because tools like Centreon do nto generate all
#configuration (onnly the shinken.cfg part)
SHINKENSPECIFICCFG="$ETC$/shinken-specific.cfg"

# The command to launch
ARBITERDAEMON=$SCRIPTS_BIN$/shinken-arbiter

#The ARBITER PID
ARBITERPID=$(grep lock_file $SHINKENCFG | cut -d= -f2)

###PART FOR SCHEDULER
# The place of the scheduler daemon configuration
SCHEDULERCFG="$ETC$/schedulerd.ini"

# The command to launch
SCHEDULERDAEMON=$SCRIPTS_BIN$/shinken-scheduler

#The SCHEDULER PID
SCHEDULERPID="$VAR$/schedulerd.pid"


###PART FOR POLLER
# The place of the poller daemon configuration
POLLERCFG="$ETC$/pollerd.ini"

# The command to launch
POLLERDAEMON=$SCRIPTS_BIN$/shinken-poller

#The poller pid
POLLERPID="$VAR$/pollerd.pid"


###PART FOR REACTIONNER
# The place of the reactionner daemon configuration
REACTIONNERCFG="$ETC$/reactionnerd.ini"

# The command to launch
REACTIONNERDAEMON=$SCRIPTS_BIN$/shinken-reactionner

#The reactionner pid
REACTIONNERPID="$VAR$/reactionnerd.pid"


###PART FOR BROKER
# The place of the broker daemon configuration
BROKERCFG="$ETC$/brokerd.ini"

# The command to launch
BROKERDAEMON=$SCRIPTS_BIN$/shinken-broker

#The broker pid
BROKERPID="$VAR$/brokerd.pid"

# nicelevel to run nagios daemon with
NICENESS=5

