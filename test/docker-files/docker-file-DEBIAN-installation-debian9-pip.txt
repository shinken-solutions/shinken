FROM debian:9
MAINTAINER Jean Gabes <naparuba@gmail.com>

RUN        apt-get update && apt-get install -y python
RUN        apt-get install -y python-pip
RUN        apt-get install -y python-setuptools
RUN         apt-get install -y python-pycurl

ADD        . /root/shinken-framework

WORKDIR     /root
RUN     tar cfz opsbro-0.1.tar.gz shinken-framework

# local pip install
#ENTRYPOINT     DEBUG_INSTALL=1 pip -vvv install opsbro --no-index --find-links file://opsbro-0.1.tar.gz && cd shinken-framework && test/test_one_linux_installation.sh && test/assert_group.sh "debian"
ENTRYPOINT     DEBUG_INSTALL=0 pip install shinken-0.1.tar.gz && cd shinken-framework && test/test_one_linux_installation.sh