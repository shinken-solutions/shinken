define service {
use				service-nwc-link-state
host_name			test_host_0
service_description		INTERFACE-$KEY$-STATE
duplicate_foreach		_iface
}
define service {
name				service-dependencies
register			0
service_dependencies		test_host_0,INTERFACE-$KEY$-STATE
duplicate_foreach		_iface
}
define service {
use				service-dns-resolver,service-dependencies
host_name			test_host_0
service_description		DNS-RESOLVER
}