{
"name":"switch",

"description":"Standard switch checks. Checks are done by check_nwc_health.",

"path":"network/",

"macros":{
        "_SWITCH_CPU_LOAD_CRIT": {"type":"percent",
                             "description":"Check the CPU load of the device"
                             },
        "_SWITCH_CPU_LOAD_WARN": {"type":"percent",
                             "description":"Check the CPU load of the device"
                             },
        "_SWITCH_MEMORY_USAGE_CRIT": {"type":"percent",
                             "description":"Check the memory usage of the device"
                             },
        "_SWITCH_MEMORY_USAGE_WARN": {"type":"percent",
                             "description":"Check the memory usage of the device"
                             }
         }
}
